import { Bell, ChevronDown, LogOut } from 'lucide-react';
import { useAuth } from '../auth/useAuth';
export function TopHeader() { const { user, logout } = useAuth(); return <header className="top-header"><h1>Central Procurement Requisition Portal</h1><div className="top-header__actions"><button className="notification-button" aria-label="Notifications"><Bell size={20} /><b>3</b></button><span className="header-separator" /><span className="avatar">{user?.fullName?.split(' ').map(x => x[0]).slice(0, 2).join('')}</span><div className="profile-copy"><strong>{user?.fullName}</strong><small>{user?.designation || user?.roles[0]?.replaceAll('_', ' ')} <i>•</i> {user?.orgUnitName}</small></div><ChevronDown size={15} /><button className="signout-icon" onClick={logout} title="Sign out"><LogOut size={17} /></button></div></header>; }
