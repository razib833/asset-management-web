import{axiosClient}from'../axiosClient';import{endpoints}from'../endpoints';import type{ApiResponse,AuthenticatedUser,LoginResponse}from'../../types';export const authApi={async login(email:string,password:string){const{data}=await axiosClient.post<ApiResponse<LoginResponse>>(endpoints.auth.login,{email,password});if(!data.success||!data.data)throw new Error(data.message);return data.data},async me(){const{data}=await axiosClient.get<ApiResponse<AuthenticatedUser>>(endpoints.auth.me);if(!data.success||!data.data)throw new Error(data.message);return data.data}};
